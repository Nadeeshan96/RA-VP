"""v4_sequence_classifiers — deep direct-classification sequence baseline family.

LSTM-CLS, GRU-CLS, TCN-CLS trained from scratch on per-timestep engineered
features. Sibling to v2 (tabular engineered) — shares feature math but keeps
the temporal dimension.
"""
